<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_users_view_form_approval" model="ir.ui.view">
        <field name="name">res.users.form.occ.approval</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='preferences']" position="inside">
                <group string="审批组织" groups="occ_approval.group_approval_manager">
                    <field name="occ_approval_manager_id" domain="[('id', '!=', id)]"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>
